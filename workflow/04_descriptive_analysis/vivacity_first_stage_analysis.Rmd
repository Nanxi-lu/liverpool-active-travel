---
title: "Vivacity First-Stage Dissertation Analysis"
author: "Nanxi Lu"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_depth: 3
    number_sections: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  message = FALSE,
  warning = FALSE,
  fig.width = 11,
  fig.height = 6
)
```

## Purpose

This notebook is the first analysis stage after data cleaning and spatial/context joining.

It starts from the joined daily Vivacity + LSOA 2021 context table and produces:

- deprivation groups using IMD deciles;
- scheme and sensor readiness tables;
- weekly and monthly descriptive summaries;
- descriptive pre/post change tables;
- exploratory interrupted time-series model tables for schemes with reliable pre/post data;
- first dissertation plots for trends by scheme, route type, and deprivation group.

The analysis remains exploratory because matched non-intervention sensors have not yet been added. Therefore, the ITS outputs should be treated as early evidence of trajectories, not final causal estimates.

## Inputs

Main input:

```{r input-paths}
base_dir <- "/Users/lu_nanxi/CASA/Dissertation_Data"
context_dir <- file.path(base_dir, "context_data")
cutoff_context_dir <- file.path(base_dir, "Vivacity_full_day_cutoff_20260526", "context")

daily_context <- file.path(
  cutoff_context_dir,
  "vivacity_daily_lsoa2021_context_analysis_to_2026-05-26.csv"
)

daily_with_index <- file.path(
  cutoff_context_dir,
  "vivacity_daily_lsoa2021_context_with_ltp3_index_to_2026-05-26.csv"
)

input_daily <- if (file.exists(daily_with_index)) daily_with_index else daily_context
input_daily
```

The `daily_with_index` version is preferred when available because it attaches the LCR LTP3 cycling indicator by financial year. The index currently ends at 2023/24, so later rows have missing index values rather than imputed values.

## Run Analysis

The full analysis is implemented in a separate R script so it can be rerun from RStudio or the terminal.

```{r run-analysis}
source(file.path(base_dir, "first_stage_analysis_outputs.Rmd"))
```

## Output Folders

```{r output-folders}
analysis_output_dir <- file.path(base_dir, "Vivacity_R_outputs", "first_stage_analysis")
analysis_table_dir <- file.path(analysis_output_dir, "tables")
analysis_plot_dir <- file.path(analysis_output_dir, "plots")

analysis_output_dir
analysis_table_dir
analysis_plot_dir
```

## Key Tables

```{r key-tables}
scheme_readiness <- readr::read_csv(
  file.path(analysis_table_dir, "scheme_analysis_readiness.csv"),
  show_col_types = FALSE
)

sensor_readiness <- readr::read_csv(
  file.path(analysis_table_dir, "sensor_analysis_readiness.csv"),
  show_col_types = FALSE
)

pre_post_change <- readr::read_csv(
  file.path(analysis_table_dir, "pre_post_daily_mean_change_by_scheme_route.csv"),
  show_col_types = FALSE
)

its_key_terms <- readr::read_csv(
  file.path(analysis_table_dir, "exploratory_its_key_terms.csv"),
  show_col_types = FALSE
)
```

```{r scheme-readiness}
scheme_readiness |>
  dplyr::select(
    scheme_id,
    road_group,
    imd_groups,
    lsoa21_names,
    pre_calendar_days,
    post_calendar_days,
    analysis_use
  )
```

```{r pre-post-summary}
pre_post_change |>
  dplyr::select(
    scheme_id,
    route_group,
    imd_group,
    sensor_days_pre,
    sensor_days_post,
    active_per_countline_day_pre,
    active_per_countline_day_post,
    active_pct_change,
    has_pre_post
  )
```

```{r its-key-terms}
its_key_terms |>
  dplyr::select(
    scheme_id,
    route_group,
    imd_group,
    outcome,
    term,
    rate_ratio,
    conf_low,
    conf_high,
    p.value,
    pre_weeks,
    post_weeks
  )
```

## Plots

The script saves publication-ready PNGs into the plots folder. The most useful initial figures are:

- `scheme_analysis_readiness.png`
- `monthly_mode_by_scheme_imd_group.png`
- `weekly_active_travel_by_scheme_imd_group.png`
- `descriptive_pre_post_change_by_scheme_route.png`
- `exploratory_its_active_travel_rate_ratios.png`

```{r plot-preview-readiness, echo=FALSE, out.width="100%"}
knitr::include_graphics(file.path(analysis_plot_dir, "scheme_analysis_readiness.png"))
```

```{r plot-preview-weekly, echo=FALSE, out.width="100%"}
knitr::include_graphics(file.path(analysis_plot_dir, "weekly_active_travel_by_scheme_imd_group.png"))
```

```{r plot-preview-prepost, echo=FALSE, out.width="100%"}
knitr::include_graphics(file.path(analysis_plot_dir, "descriptive_pre_post_change_by_scheme_route.png"))
```

## How To Use These Outputs

Use `scheme_analysis_readiness.csv` to decide which schemes are suitable for pre/post modelling. At present:

- schemes with enough reliable pre/post data can enter exploratory ITS;
- schemes with only post-installation data should be described as post-opening trajectories;
- none of these should yet be treated as full causal estimates until matched non-intervention sensors are added.

Use `pre_post_daily_mean_change_by_scheme_route.csv` as a descriptive table for reporting early changes in average daily walking/cycling counts.

Use `exploratory_its_key_terms.csv` to report model direction and uncertainty. The most important ITS terms are:

- `post`: immediate level change after the intervention month;
- `week_after`: change in weekly trend after the intervention month.

## Next Analysis Extension

The next methodological step is to add matched non-intervention countlines or areas. Once those are available, this notebook can be extended from single-series ITS to a controlled interrupted time-series or difference-in-differences design.
