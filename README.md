# Output

Open `index.html` first. The links inside the page are relative to this folder, so they work after cloning the GitHub repository or downloading this folder.

This package contains selected analysis summaries, maps, visual outputs, chapter drafts, and workflow files. It is intended for progress reporting and explanation, not as a raw data release.
